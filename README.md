# medadvisor-cloud

Cloud backend + mentor dashboard for MedAdvisor. Start with CLAUDE.md (context)
and PLAN.md (milestones + status — the source of truth).

- `server/` — Cloud Run API (Node/Express)
- `dashboard/` — mentor web app (framework picked in MC4)
- `infra/` — GCP runbooks + deploy notes
